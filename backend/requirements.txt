# AgendaScope 观澜 后端依赖（Python 3.11）
# Web 框架与配置
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-multipart>=0.0.9

# 数据库与迁移
sqlalchemy>=2.0.29
psycopg2-binary>=2.9.9
alembic>=1.13
pgvector>=0.2.5

# 缓存与队列
redis>=5.0

# 认证与安全
python-jose[cryptography]>=3.3
bcrypt>=4.1,<6

# 采集与抽取
feedparser>=6.0.10
trafilatura>=1.8
readability-lxml>=0.8.1
requests>=2.31
lxml>=5.0
playwright>=1.43

# NLP 管线（Phase 2 M2-1）
fasttext-predict>=0.9.2    # lid.176 纯推理预编译 wheel（免 C++ 编译）
sentence-transformers>=2.7,<4
elasticsearch>=8.13,<9

# 聚类引擎（Phase 2 M2-2）
bertopic>=0.17,<0.18       # UMAP+HDBSCAN+c-TF-IDF 主线聚类
scikit-learn>=1.4          # Agglomerative 硬阈值对照策略
jieba>=0.42                # 中文分词（c-TF-IDF/关键词降级匹配）

# 统计佐证（Phase 3 M3-3，T3.10）
statsmodels>=0.14          # Granger 因果检验（grangercausalitytests）
scipy>=1.11                # Pearson 相关 + t 分布 survival function
numpy>=1.26                # 数值序列（bertopic/torch 已传递依赖，显式声明对齐版本）

# 日志
structlog>=24.1

# 报告导出（Phase 4 M4-3，T4.17）
reportlab>=4.2        # PDF 生成（内置 STSong-Light CID 字体支持中文水印/正文）
python-docx>=1.1      # Word 生成

# LLM 本地推理（M2-3）
# CPU 环境安装 torch 建议: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
transformers>=4.43
huggingface_hub>=0.24

# 测试
pytest>=8.1
pytest-asyncio>=0.23
httpx>=0.27
