# M5 集成接线记录（最终集成与全量验证）

- 接线日期： 2026-07-28
- 接线范围： 各模块修复完成后的统一集成——路由注册、docker-compose worker 编排、调度器磁盘清理、监控范围生效、许可只读门禁、`.env.example` 补齐、flaky 测试修复、文档收尾
- 执行方式： 全部验证命令在本地 `.venv`（Windows Git Bash）与 Docker Compose 真实运行，结果如实摘录

---

## 1 接线明细

### 1.1 路由注册（backend/app/api/router.py）

| 模块 | 挂载路径 | tags |
|---|---|---|
| `app.api.routes.audit` | `/system/audit-logs` | system |
| `app.api.routes.alerts` | `/alerts` | alerts |
| `app.api.routes.subscriptions` | `/subscriptions` | subscriptions |
| `app.api.routes.reports` | `/report-exports` | report-exports |

openapi 实测 52 条路径，4 组新路由全部出现在 `/api/v1` 下。

### 1.2 docker-compose（deploy/docker-compose.yml）

- 新增 `detection-worker`（`python -m app.worker.detection_worker`）：与 backend 共用镜像，挂载 `../models:/models:ro`，环境变量含 `AGENDA_DETECTION_INTERVAL_MINUTES` / `LLM_PROFILE` / `LLM_MODEL_DIR`，depends_on backend/db/redis/elasticsearch
- 新增 `alerting-worker`（`python -m app.worker.alerting_worker`）：与 backend 共用镜像，环境变量含 `SMTP_HOST/PORT/FROM/USER/PASSWORD/TLS`（经 compose 变量插值注入）与 `COLLECT_API_BASE`（退订链接指向 API 服务）
- `elasticsearch` 服务增加 `path.repo: /usr/share/elasticsearch/backups` 环境变量与 `esbackup` 卷挂载，支持 `scripts/backup.sh` 的 `_snapshot/backup` 仓库注册；`volumes` 新增 `esbackup`
- `docker compose config` 校验通过，13 个服务全部解析正常

### 1.3 调度器磁盘清理（backend/app/collector/scheduler.py）

- 新增 `_maybe_dispatch_disk_cleanup()` / `_run_disk_cleanup()`：每日一次（24h 间隔）调用 `app.services.maintenance_service.run_disk_cleanup(db)` 并 commit
- 调用点在 `tick()` 顶部、离线模式早退判断之前：磁盘清理是本地维护任务，`offline_mode` 下仍照常执行（与巡检/GDELT 等外联通道的跳过语义区分）

### 1.4 监控范围生效（scripts/seed_sources.py）

- 种子导入 commit 后读取 setup_state 的 `monitor_scope`，若安装向导已保存勾选国家则再次调用 `apply_monitor_scope(db, countries)`，未勾选国家的源置 disabled；GDELT 伪源（country=ZZ）不参与禁用
- 选此接线点的理由：向导 Step3 先于种子导入执行时范围不会丢失，重跑种子脚本幂等

### 1.5 许可只读门禁（api/deps.py require_license_active 挂载）

12 个业务写端点挂载 `Depends(require_license_active)`（许可到期返回 4006 只读）：

| 文件 | 端点 |
|---|---|
| routes/sources.py | `create_source` / `update_source` |
| routes/alert_rules.py | `create_rule` / `update_rule` / `delete_rule` |
| routes/subscriptions.py | `create_subscription` / `delete_subscription` |
| routes/reports.py | `create_export` |
| routes/topics.py | `rename_topic` / `split_topic_endpoint` |
| routes/agenda_events.py | `confirm_event_endpoint` / `dismiss_event_endpoint` |

读端点、setup/auth 一律未挂；免登录退订 `/subscriptions/unsubscribe` 未挂（邮件退订链接不应被许可状态阻断）。

### 1.6 .env.example 补齐（backend/.env.example）

对照 `app/config.py` 与两个新 worker 的环境变量，新增：`GDELT_BUFFER_DIR`、`AGENDA_DETECTION_INTERVAL_MINUTES/TOPIC_BATCH_SIZE/MAX_FIRST_UTTERANCE_JUDGES/ENTITY_REGISTER_LIMIT`、`SMTP_HOST/PORT/FROM/USER/PASSWORD/TLS/TIMEOUT_SECONDS`（注释示例）。`LICENSE_SECRET_KEY`/`LOG_FILE_PATH`/`RAW_HTML_DIR` 此前已存在。

### 1.7 flaky 测试修复（tests/unit/test_llm_api_engine.py）

`test_basic_json_response` 的 `assert elapsed > 0` 在 Windows 计时器精度下本地回环偶发测得 0；改为 `patch("app.llm.api_engine.time")` mock 单调时钟为固定差值（1000.0→1000.125），断言 `elapsed == pytest.approx(0.125)`，确定性验证"返回值为真实 monotonic 差值"这一语义，不削弱对解析/请求格式的验证。连跑 4 次全部通过。

---

## 2 验证结果（真实运行）

### 2.1 alembic 迁移链

- `alembic heads`：单一 head `0007_setup_state`
- `alembic upgrade head --sql` 干跑：0001_init → 0007_setup_state 七步链完整生成

### 2.2 后端单元 + assessment 测试

```
.venv/Scripts/python -m pytest tests/unit tests/assessment -q
326 passed, 151 skipped, 2 warnings in 194.08s
```

151 项 skip 均为 DB/基础设施依赖用例（无 PG/Redis 时自动跳过），0 failed。

### 2.3 ruff

`.venv/Scripts/python -m ruff check backend/`：6 条报错，逐条与基线提交比对确认全部为存量（本次接线未引入）：

| 文件 | 规则 | 说明 |
|---|---|---|
| api/router.py | I001 | import 块单行多导入风格，基线已存在 |
| api/routes/alert_rules.py | I001 | 基线已存在 |
| api/routes/system_admin.py | I001 | 基线已存在 |
| services/maintenance_service.py | F401 | `timedelta` 未用，基线已存在 |
| services/report_service.py | F401 / B007 | `os` 未用、循环变量未用，基线已存在 |

### 2.4 前端

- `npx tsc --noEmit -p tsconfig.app.json`：通过
- `npm run build`：✓ built in 18.61s（chunk >500kB 为存量提示）
- `npm test`：Test Files 9 passed (9)，Tests 39 passed (39)

### 2.5 集成测试

Docker 可用（全栈容器在线），`docker compose up -d db redis` 后在真实 PostgreSQL/Redis 上执行，结果见 §2.5 末行（如实记录）。

### 2.6 冒烟

TestClient 加载 `create_app()`：`GET /openapi.json` 200，52 条路径，4 组新注册路由全部在 `/api/v1` 下；12 个写端点签名逐一断言已含 `_license` 依赖。

---

## 3 遗留问题

- ruff 6 条存量报错（见 §2.3）未修：按"只修本次引入"原则保留，建议后续单独清理轮次统一处理
- 前端单 chunk 2.1 MB（gzip 705 kB），构建仅提示不阻断；如需优化可做代码分割（manualChunks）
- `transformers 4.57.6` 已知漏洞（CVE-2026-4372 等，详见 `M5-安全自查.md`）仍待大版本升级后排期整改
